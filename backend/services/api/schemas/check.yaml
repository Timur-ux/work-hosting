components:
  schemas:
    WorkRequest:
      type: object
      additionalProperties: false
      properties:
        work-type:
          type: string
          default: LR
        old-work-number:
          type: integer
          minimum: 1
      required:
        - work-type
        - old-work-number

    SendWorkResponse:
      type: string

